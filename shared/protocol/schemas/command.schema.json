{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://victus.natsec/protocol/0.3.0/command.schema.json",
  "title": "VICTUS command payload",
  "description": "Aligned with drone_command_policy.json — the dictionary the on-board LLM responds to.",
  "type": "object",
  "required": ["drone_id", "verb", "priority", "expires_at", "params"],
  "additionalProperties": false,
  "properties": {
    "drone_id": { "type": "string" },
    "verb": {
      "type": "string",
      "enum": [
        "ABORT",
        "RTB",
        "GOTO",
        "ALTITUDE",
        "LOITER",
        "SEARCH",
        "OBSERVE",
        "REPORT",
        "TRACK",
        "IDENTIFY",
        "ASSIGN_MISSION"
      ],
      "description": "Policy command (priority 1–10) or ASSIGN_MISSION (operator-issued mission delivery)."
    },
    "priority": {
      "type": "string",
      "enum": ["ROUTINE", "PRIORITY", "IMMEDIATE", "FLASH"]
    },
    "roe": {
      "type": "string",
      "description": "Optional ROE profile id."
    },
    "expires_at": {
      "type": "string",
      "format": "date-time"
    },
    "supersedes": {
      "type": "string",
      "format": "uuid",
      "description": "Optional message_id of a command this one replaces."
    },
    "params": {
      "type": "object",
      "description": "Verb-specific parameters per the policy schema. Validated per-verb at the application layer; see docs/PROTOCOL.md and drone_command_policy.json."
    }
  }
}
